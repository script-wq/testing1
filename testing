-- Create GUI
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "LimitHubLoader"
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true
ScreenGui.Parent = game:GetService("CoreGui")

-- Main Frame (Centered)
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 520, 0, 260)
mainFrame.Position = UDim2.new(0.5, 0, 0.5, 0)
mainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
mainFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
mainFrame.BorderSizePixel = 4
mainFrame.Parent = ScreenGui

-- Rainbow Border Animation
task.spawn(function()
	local hue = 0
	while mainFrame and mainFrame.Parent do
		hue = (hue + 1) % 256
		mainFrame.BorderColor3 = Color3.fromHSV(hue / 255, 1, 1)
		wait(0.05)
	end
end)

-- Title with Emojis 🌙🐾
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -20, 0.25, 0)
title.Position = UDim2.new(0, 10, 0, 10)
title.BackgroundTransparency = 1
title.Text = "🌟 Limit Hub - Pet Spawner 🐾 & Moon Cat Freeze 🌙"
title.TextColor3 = Color3.fromRGB(255, 215, 0)
title.Font = Enum.Font.GothamBlack
title.TextScaled = true
title.Parent = mainFrame

-- Animated Loading Text with Emojis ⏳
local loadingText = Instance.new("TextLabel")
loadingText.Size = UDim2.new(1, 0, 0.2, 0)
loadingText.Position = UDim2.new(0, 0, 0.4, 0)
loadingText.BackgroundTransparency = 1
loadingText.TextColor3 = Color3.fromRGB(135, 206, 250) -- Sky blue
loadingText.Font = Enum.Font.GothamBold
loadingText.TextScaled = true
loadingText.Text = "⏳ Loading"
loadingText.Parent = mainFrame

-- Animate Dots
task.spawn(function()
	local dots = 0
	while loadingText and loadingText.Parent do
		loadingText.Text = "⏳ Loading" .. string.rep(".", dots)
		dots = (dots + 1) % 4
		wait(0.5)
	end
end)

-- Loading Bar Background
local barBg = Instance.new("Frame")
barBg.Size = UDim2.new(0.8, 0, 0.08, 0)
barBg.Position = UDim2.new(0.1, 0, 0.75, 0)
barBg.BackgroundColor3 = Color3.fromRGB(220, 220, 220)
barBg.BorderSizePixel = 0
barBg.ClipsDescendants = true
barBg.Parent = mainFrame

-- Loading Bar Fill
local barFill = Instance.new("Frame")
barFill.Size = UDim2.new(0, 0, 1, 0)
barFill.Position = UDim2.new(0, 0, 0, 0)
barFill.BackgroundColor3 = Color3.fromRGB(255, 100, 200)
barFill.BorderSizePixel = 0
barFill.Parent = barBg

-- Animate bar for 30 seconds (NOT 180)
local duration = 30
local startTime = tick()

task.spawn(function()
	while tick() - startTime < duration do
		local progress = math.clamp((tick() - startTime) / duration, 0, 1)
		barFill.Size = UDim2.new(progress, 0, 1, 0)

		-- Dynamic rainbow fill color
		local hue = (tick() * 0.5) % 1
		barFill.BackgroundColor3 = Color3.fromHSV(hue, 1, 1)

		wait(0.05)
	end

	-- Remove GUI after loading
	ScreenGui:Destroy()

	-- ✅ Run your script after loading:
	loadstring(game:HttpGet(('https://raw.githubusercontent.com/FakeModz/LimitHub/refs/heads/main/LimitHub_Luarmor_E.lua')))()
end)
